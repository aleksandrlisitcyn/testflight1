[tool.black]
line-length = 100
target-version = ["py310"]

[tool.ruff]
line-length = 100
extend-exclude = ["app/tests", "app/api/admin.py"]

[tool.ruff.lint]
select = ["E", "F", "B", "I"]
ignore = ["B008"]

[tool.pytest.ini_options]
addopts = "--maxfail=1 --disable-warnings"
testpaths = ["tests"]
